[workspace]
authors = ["Tien Chueh Wang <f74169@gmail.com>"]
channels = ["https://conda.modular.com/max-nightly", "conda-forge"]
name = "inmojomni"
platforms = ["linux-64"]
version = "0.1.0"
preview = ["pixi-build"]

# The ludens-engine subsystems (ECS, geometry, physics, collision, scheduler,
# spatial, harness, oop) now live in their own repo at ~/Projects/ludens-engine.
# What remains here are the standalone experiments: concurrent/, logic/, rx/.
[tasks]

[dependencies]
modular = ">=26.4.0.dev2026051806,<27"
mojo = ">=1.0.0b2.dev2026051806,<2"
